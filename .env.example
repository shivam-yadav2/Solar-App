# Android emulator default (local development)
EXPO_PUBLIC_API_URL=http://10.0.2.2:3000/api

# For a physical device on the same Wi-Fi, replace 10.0.2.2 with the
# development computer's LAN address. In EAS preview/production environments,
# set this to the public HTTPS backend URL, including /api.
